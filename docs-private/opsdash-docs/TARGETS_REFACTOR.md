# Targets Service Refactor

Status: completed.

- Modules live under `opsdash/src/services/targets/` (`config`, `progress`, `forecast`, `math`).
- Tests live under `opsdash/test/*targets*.test.ts`.

Keep this doc short; add notes only if the module layout changes.
