# Sidebar Range Controls

- Range controls live in the sidebar header (week/month toggle + prev/next).
- When the sidebar is collapsed, the rangebar UI provides the minimal controls.
- Keyboard shortcuts mirror the same actions (see `KEYBOARD_SHORTCUTS.md`).
